---
# Check with `systemctl status qemu-guest-agent`
- name: Enable and start qemu-guest-agent
  become: true
  ansible.builtin.systemd:
    name: qemu-guest-agent
    enabled: true
    state: started
